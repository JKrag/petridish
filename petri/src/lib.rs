//! `petri` — the Rust/ratatui reimplementation of the interactive dashboard.
//! Spec: `petri/SPEC.md`. Scaffold only; S4 fills in the walking skeleton
//! (real terminal, event loop, poll timer, panic hook — petri/SPEC.md §9 S4).

/// Placeholder entry point for the walking skeleton. S4 replaces this body;
/// `main.rs` calls it the same way `swab`'s `main.rs` calls `swab::cli::main()`.
pub fn run() -> std::io::Result<()> {
    todo!("S4: real terminal init, event loop, state-file read, panic hook")
}
